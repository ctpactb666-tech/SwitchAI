<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_home_cosmic">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_container"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_settings" />

    <FrameLayout
        android:id="@+id/bottom_nav_container"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_nav_height"
        android:layout_gravity="bottom"
        android:layout_marginStart="@dimen/bottom_nav_side_margin"
        android:layout_marginEnd="@dimen/bottom_nav_side_margin"
        android:layout_marginBottom="@dimen/bottom_nav_margin"
        android:background="@drawable/bg_bottom_nav_neon"
        android:elevation="8dp">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            app:elevation="0dp"
            app:itemActiveIndicatorStyle="@style/SwitchAI.BottomNavIndicator"
            app:itemIconSize="24dp"
            app:itemIconTint="@color/bottom_nav_item"
            app:itemTextAppearanceActive="@style/SwitchAI.BottomNavText.Active"
            app:itemTextAppearanceInactive="@style/SwitchAI.BottomNavText.Inactive"
            app:itemTextColor="@color/bottom_nav_item"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/main_bottom_nav" />
    </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
